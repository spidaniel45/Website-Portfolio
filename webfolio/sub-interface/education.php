<section id="education" class="content-section">
    <h2 class="fw-bold mb-4"><i class="bi bi-mortarboard me-2"></i>Education</h2>

    <style>
    /* ── Education Cards ─────────────────────────────────── */
    .education-card {
        background: var(--surface-2);
        border: 2px solid var(--brand-dark);
        border-radius: 12px;
        padding: 20px;
        margin-bottom: 20px;
        transition: transform 0.2s ease, border-color 0.2s ease;
    }
    .education-card:hover {
        transform: translateY(-2px);
        border-color: var(--brand-mid);
    }
    .education-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 12px;
        flex-wrap: wrap;
        gap: 10px;
    }
    .education-degree {
        font-size: 1.2rem;
        font-weight: 600;
        flex: 1;
        min-width: 200px;
    }
    .education-year {
        font-size: 0.9rem;
        color: var(--text-muted);
        white-space: nowrap;
    }
    .education-institution {
        font-size: 1.1rem;
        font-weight: 500;
        color: var(--text-primary);
        margin: 6px 0;
    }
    .education-details {
        font-size: 0.9rem;
        color: var(--text-muted);
        margin: 4px 0;
        line-height: 1.5;
    }
    .education-details i {
        width: 16px;
        text-align: center;
        margin-right: 4px;
    }
    </style>

    <div class="education-card">
        <div class="education-header">
            <div class="education-degree">Bachelor of Technical-Vocational Teacher Education</div>
            <div class="education-year">Expected 2027</div>
        </div>
        <div class="education-institution">Rizal Technological University</div>
        <div class="education-details">
            <i class="bi bi-building"></i> Major in Computer System Servicing
        </div>
        <div class="education-details">
            <i class="bi bi-geo-alt"></i> Boni Ave, Mandaluyong, 1550 Metro Manila
        </div>
    </div>
</section>
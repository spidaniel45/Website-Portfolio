<section id="certifications" class="content-section">
    <h2 class="fw-bold mb-4"><i class="bi bi-award me-2"></i>Certifications</h2>

    <style>
    /* ── Certification Cards ─────────────────────────────── */
    .certification-card {
        background: var(--surface-2);
        border: 2px solid var(--brand-dark);
        border-radius: 12px;
        padding: 20px;
        margin-bottom: 20px;
        transition: transform 0.2s ease, border-color 0.2s ease;
    }
    .certification-card:hover {
        transform: translateY(-2px);
        border-color: var(--brand-mid);
    }
    .certification-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 12px;
        flex-wrap: wrap;
        gap: 10px;
    }
    .certification-title {
        font-size: 1.1rem;
        font-weight: 600;
        flex: 1;
        min-width: 200px;
    }
    .certification-date {
        font-size: 0.9rem;
        color: var(--text-muted);
        white-space: nowrap;
    }
    .certification-provider {
        font-size: 0.95rem;
        font-weight: 500;
        color: var(--text-primary);
        margin: 6px 0;
    }
    .certification-details {
        font-size: 0.9rem;
        color: var(--text-muted);
        margin: 4px 0;
        line-height: 1.5;
    }
    .certification-details i {
        width: 16px;
        text-align: center;
        margin-right: 4px;
    }
    </style>

    <div class="certification-card">
        <div class="certification-header">
            <div class="certification-title">Computer Systems Servicing NCII</div>
            <div class="certification-date">Completed 2026</div>
        </div>
        <div class="certification-provider">MindTech Training & Development Institute, INC. Pasig, City</div>
    </div>
</section>